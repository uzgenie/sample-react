import Comparison from "./comparison";
import Tredns from "./trends";
import Reports from "./reports";
const reducers = {
  comparison: Comparison,
  tredns: Tredns,
  reports: Reports,
};

export default reducers;
